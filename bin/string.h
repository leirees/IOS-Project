#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * @brief Returns the concatenation of two strings, no matter what size they have.
 * 
 * @param dest 
 * @param src 
 * 
 * @return char* the concat. string. 
 */
char *_strcat(char *dest, const char *src);